'use strict';

const assert   = require('assert');
const mocha = require('mocha');
const server = require('../server.js');

desribe('\\nick', function(){
  describe('nickname changing', function() {
    it('should change nickname', function(done) {
      
      done();
    });
  });
});
